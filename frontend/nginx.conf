server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;

    # SPA fallback: react-router owns client-side routing, so every path
    # that isn't a real static file resolves to index.html.
    location / {
        try_files $uri /index.html;
    }
}
