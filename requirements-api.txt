# Runtime dependencies for the deployed API only (Dockerfile) -- deliberately
# narrower than pyproject.toml's full dependency list, which also covers
# notebooks/dbt tooling (jupyter, pandas, matplotlib, pyarrow) that has no
# reason to ship inside the production image.
duckdb>=1.0
anthropic>=0.75
fastapi>=0.115
uvicorn[standard]>=0.32
slowapi>=0.1.9
python-dotenv>=1.0
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-exporter-otlp-proto-http>=1.27
